name: batch-ci
on:
  pull_request:
    paths: ["use_cases/batch/**"]
jobs:
  test-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: pip install -e .[local] && pytest && databricks bundle validate"
